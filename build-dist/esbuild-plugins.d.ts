export namespace dedupBn {
    const name: string;
    function setup({ onResolve }: {
        onResolve: any;
    }): void;
    function setup({ onResolve }: {
        onResolve: any;
    }): void;
}
